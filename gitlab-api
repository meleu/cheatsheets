# a function to access the gitlab api from a pipeline job
gitlabApi() {
  local path="$1"
  curl \
    --silent \
    --header "PRIVATE-TOKEN: ${TOKEN}" \
    "${CI_API_V4_URL}/${path}"
}


# get the log of a pipeline job
gitlabApi "/projects/${projectId}/jobs/${jobId}/trace"


